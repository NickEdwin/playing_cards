<h1>Card Shuffler!</h1>

<%= button_to 'Shuffle the cards!', "/shuffled", class: "shufflebtn", method: :get %>

<% @deck.cards.each do |card| %>
  <p>The <%= card.rank %> of <%= card.suit %></p>
<% end %>
