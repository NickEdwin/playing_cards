<h1>Card Shuffler!</h1>

<%= button_to 'Shuffle the cards!', "/shuffled", method: :get %><br>
<%= button_to 'Unshuffle the cards!', "/", method: :get %>


<% @cards.each do |card| %>
  <p>The <%= card.rank %> of <%= card.suit %></p>
<% end %>
